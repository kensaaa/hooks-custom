# Hook - useForm


## Llamando al hook
```javascript
const initialForm = {
    name:'kevin',
    age:21,
    email:'ke.samata.ramos@gmail.com'
}

const [ values, handleInputChange, reset ] = useForm(initialForm)
```
